- if activity.trackable
  .event
    .label
      %i.circular.blue.comment.icon
    .content
      .date
        = "#{format_time(activity.created_at)} geleden"
      .summary
        = link_to((activity.owner.full_name if activity.owner), '#')
        = activity.trackable.title
- else
  /iets anders hier
