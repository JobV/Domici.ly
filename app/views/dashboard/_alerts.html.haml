%h1.ui.header
  .content
    Meldingen
    .sub.header 
      De laatste meldingen voor de bewoners van XYZ. Alleen bestuursleden kunnen meldingen aanmaken.

.ui.divider

.row
  .small-12.small-centered.column.text-center
    = link_to new_alert_path do
      .ui.labeled.mini.icon.button
        %i.add.icon
        Nieuwe Melding

.ui.stackable.items
  - alerts.each do |alert|
    .item.alert-item{ href: "/alerts/#{alert.id}" }
      .content
        .meta
          = format_date alert.created_at
        .name
          = alert.title
        %p.description
          = alert.body
      .extra
        199 comments
