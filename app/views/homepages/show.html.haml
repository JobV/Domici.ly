- @active_tab = 'homepage'
- unless current_user
  .logo
    Domici.ly

.row
  .small-12.column

    .big-header
      -if @hoa
        %h1.title
          = @hoa.name
      -else
        %h3
          Dit subdomein is nog vrij!

      - unless current_user
        = link_to new_user_session_path, class: 'ui huge positive button' do
          Log in

