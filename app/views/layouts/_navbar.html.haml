- if signed_in?
  .ui.secondary.pointing.menu
    %a.active.item{ href: '/' }
      %i.home.icon
      Homely
    - if admin?
      %a.item{ href: '/superadmin'}
        %i.settings.icon
        Admin
    .right.menu
      .ui.dropdown.item
        = current_user.name
        %i.icon.dropdown
        .menu
          %a.item
            %i.edit.icon
            Mijn profiel
          %a.item
            %i.sign.out.icon
            Log uit

      = link_to 'Log uit', destroy_user_session_path, class: 'ui item', method: :delete
